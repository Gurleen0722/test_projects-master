<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8"/>
        <meta name=" viewport" content=" width=device-widtg, initial-scale=1.0" />

    </head>
    <body>
        <h1>Login Page</h1>
        <form id ="login-form">
            <input type="email" id="email" placeholder="email"/>
            <input type="password" id="password" placeholder="password"/>
            <button id="login-button" id="login-button" onclick="loginSubmit(event)">Submit</button>
        </form>

        <script>
            let isSubmittedSuccessfully = false;

            function loginSubmit(event){
                event.preventDefault();

                //Check if the form has been submitted already
                if(isSubmittedSuccessfully){
                    alert('You have submitted too many times');
                    return;
                }

                const email = document.getElementaryByID("email").value;
                const password = document.getElementaryByID("password").value;

                if(email == "test@example.com" && password =="password"){
                    alert('login successfully!');

                } else {
                    alert( 'login unsuccessfully! please try again');
                }
                       }
        </script>
    </body>
</html>
